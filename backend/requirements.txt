# Backend dependencies. Add per module as it is implemented and verified against
# the primary docs (AGENTS.md §7).

httpx2>=2.12
google-genai>=1.0
fastapi>=0.141
uvicorn>=0.52
google-adk>=2.9
mcp>=2.2
a2ui-agent-sdk>=0.6
jsonschema>=4.18
referencing>=0.30
edge-tts>=6.1
# Optional STT fallback (heavy: pulls ctranslate2/onnxruntime). Install it only
# where faster-whisper is wanted; the adapter imports it lazily:
# faster-whisper>=1.0
